<div class="sub-heading">
	<h2>{% block title %}Admin {{ brand }}{% endblock %} 📋</h2>
</div>

<card>
	<a btn href="{{ path('admin.leagues.list') }}">Leagues</a>
	<a btn href="{{ path('admin.clubs.list') }}">Clubs</a>
	<a btn href="{{ path('admin.jerseys.list') }}">Jerseys</a>
	<a btn href="{{ path('admin.sellers.list') }}">Sellers</a>
</card>
