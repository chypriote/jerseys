{% extends 'admin/base.html.twig' %}

{% set h2 = 'Form example' %}

{% block title %}{{ h2 }} | {{ brand }}{% endblock %}

{% block body %}
    <grid>
        <div col="2/3">
            <table>
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Slug</th>
                    <th>Logo</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for league in leagues %}
                    <tr>
                        <td>{{ league.id }}</td>
                        <td>{{ league.name }}</td>
                        <td>{{ league.slug }}</td>
                        <td><img src="{{ league.logo }}" alt="{{ league.name }}"/></td>
                        <td>
                            <button>add offer</button>
                            <button>edit</button>
                            {% if league.clubs.count == 0 %}
                                <button>delete</button>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div col="1/3">

            {% if form.vars.submitted and form.vars.valid %}
                <card>
                    <h3>Submitted and valid values ✨</h3>

                    <table>
                        <tr>
                            <th>Name</th>
                            <td>{{ dto.name }}</td>
                        </tr>

                        <tr>
                            <th>Slug</th>
                            <td>{{ dto.slug }}</td>
                        </tr>

                        <tr>
                            <th>Logo</th>
                            <td>{{ dto.logo }}</td>
                        </tr>
                    </table>

                    <a href="{{ path('create_league_action') }}" btn primary>Retry</a>
                </card>
            {% else %}
                <h2>Create a new league 🔏</h2>
                <fieldset>
                    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
                </fieldset>
            {% endif %}
        </div>
    </grid>
{% endblock %}
